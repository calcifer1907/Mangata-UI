{
        "name": "vite-fullstack",
        "workspaces": [
                "Client",
                "Server"
        ],
        "scripts": {
                "dev:Client": "npm --prefix Client run dev -- --host",
                "dev:Server": "npm --prefix Server run dev -- --host",
                "dev": "concurrently \"npm run dev:Client\" \"npm run dev:Server\""
        },
        "devDependencies": {
                "concurrently": "^8.2.2"
        }
}
